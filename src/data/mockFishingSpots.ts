/**
 * @deprecated Use `@/data/fishing-spots/mockFishingSpots` or fishingSpotRepository.
 * Re-export kept for compatibility during phase 4 migration.
 */
export {
  DEFAULT_SELECTED_LOCATION_ID,
  mockFishingSpots,
} from "@/data/fishing-spots/mockFishingSpots";
